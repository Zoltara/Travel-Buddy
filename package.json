{
  "name": "travel-buddy",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "build:api": "pnpm --filter @travel-buddy/types build && pnpm --filter @travel-buddy/scoring build && pnpm --filter @travel-buddy/api build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "clean": "turbo run clean && rimraf node_modules .turbo apps/web/.next apps/api/dist",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,md}\"",
    "predeploy": "pnpm clean && pnpm install && pnpm typecheck"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.6.3",
    "prettier": "^3.4.2",
    "eslint": "^9.17.0",
    "@typescript-eslint/eslint-plugin": "^8.18.1",
    "@typescript-eslint/parser": "^8.18.1",
    "rimraf": "^6.0.1",
    "vitest": "^2.1.8"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
